#ifndef DFS
#define DFS

#include <bits/stdc++.h>
#include <mpi.h>
using namespace std;

pair<bool, vector<vector<int> > > solve_dfs(pair<int,int> start, pair<int,int> end, vector<vector<int> >& maze);

#endif